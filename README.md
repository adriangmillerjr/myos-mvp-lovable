# MyOS MVP — Chat UI Polish Patch
Enhances ChatSidebar, ChatMessage, ChatInterface, useTheme, and ProfileHeader for ChatGPT-level polish.
Install: `npm i react-syntax-highlighter prismjs` then `npm run dev`.
